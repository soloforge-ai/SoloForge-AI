import 'package:flutter_test/flutter_test.dart';
import 'package:frontend/main.dart';

void main() {
  test('SoloForgeApp can be constructed', () {
    const app = SoloForgeApp();
    expect(app, isA<SoloForgeApp>());
  });
}
